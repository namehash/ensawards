---
import Error404Display from "@/components/molecules/Error404Display.astro";
import Layout from "@/layouts/Layout.astro";
import SubpageHero from "@/components/molecules/SubpageHero.astro";
import LeaderboardsHeroImage from "@/assets/leaderboardsHeroImage.png";
import { ReferralProgramEditionHeroCardPieSplit } from "@/components/atoms/cards/referralProgramEditionCard/pie-split/hero";
import { ReferralProgramEditionHeroCardRevShareCap } from "@/components/atoms/cards/referralProgramEditionCard/rev-share/hero";
import { ReferralProgramAwardModels } from "@namehash/ens-referrals";
import { ReferrerLeaderboard } from "@/components/referral-awards-program/referrers/ReferrerLeaderboard";
import { getReferralProgramEditionSummaryBySlug } from "@/utils/referralProgram";
import { ReferralProgramLeaderboardHeroButtons } from "@/components/atoms/ReferralProgramLeaderboardHeroButtons";

export const prerender = false;

const { editionSlug } = Astro.params;

const referralProgramEditionSummary = await getReferralProgramEditionSummaryBySlug(editionSlug);

// If the referral program edition summary cannot be found based on the slug in the URL,
// set the 404 status explicitly for SEO/caching purposes and render the 404 fallback.
if (referralProgramEditionSummary === undefined) {
  Astro.response.status = 404;
}
---

{
  referralProgramEditionSummary === undefined ? (
    <Layout
      isStatic={true}
      pageMetadata={{
        title: `ENSAwards: Unrecognized ENS Referral Program Edition`,
        description: `The requested ENS Referral Program edition is not recognized.`,
        url: `https://ensawards.org/ens-referral-program/editions/${editionSlug}/leaderboard`,
      }}>
      <Error404Display
        header="Unrecognized ENS Referral Program Edition"
        description="The requested ENS Referral Program edition is not recognized."
        button={{
          text: "View all ENS Referral Program editions",
          href: "/ens-referral-program/editions",
        }}
      />
    </Layout>
  ) : (
    <Layout
      pageMetadata={{
        title: `ENSAwards: ENS Leaderboard for ${referralProgramEditionSummary.displayName} Referrers`,
        description: `Compare referrers by their performance in ${referralProgramEditionSummary.displayName}.`,
        url: `https://ensawards.org/ens-referral-program/editions/${referralProgramEditionSummary.slug}/leaderboard`,
        ogImage: `https://ensawards.org/${referralProgramEditionSummary.slug}_referral_program_edition_og_image.png`,
        twitterImage: `https://ensawards.org/${referralProgramEditionSummary.slug}_referral_program_edition_twitter_og_image.png`,
      }}>
      <SubpageHero
        header={`${referralProgramEditionSummary.displayName} leaderboard`}
        description={`Compare referrers by their performance in ${referralProgramEditionSummary.displayName}.`}
        breadcrumbs={[
          { text: "ENS Referral Program", linkHref: "/ens-referral-program" },
        ]}
        customImage={{
          source: LeaderboardsHeroImage,
          width: 371,
          height: 372,
          styles: "-top-0 right-0 opacity-10",
        }}>
        {referralProgramEditionSummary.rules.awardModel ===
        ReferralProgramAwardModels.RevShareCap ? (
          <ReferralProgramEditionHeroCardRevShareCap
            client:load
            slot="largerHeroItem"
            referralProgramEditionSummary={referralProgramEditionSummary}
          />
        ) : (
          <ReferralProgramEditionHeroCardPieSplit
            client:load
            slot="largerHeroItem"
            referralProgramEditionSummary={referralProgramEditionSummary}
          />
        )}
        <div
          slot="ctaContent"
          class="flex flex-col gap-4 justify-start items-start">
          <ReferralProgramLeaderboardHeroButtons
            client:load
            referralProgramEditionSummary={referralProgramEditionSummary}
          />
          <div class="block md:hidden bg-white/15 h-[1px] w-[calc(100vw-48px)]" />
        </div>
      </SubpageHero>
      <section class="w-full box-border h-fit flex flex-col flex-nowrap justify-start items-center max-sm:gap-6 gap-10 p-5 sm:py-[60px] sm:px-8">
        <ReferrerLeaderboard
          client:load
          referralProgramEditionSummary={referralProgramEditionSummary}
        />
      </section>
    </Layout>
  )
}
