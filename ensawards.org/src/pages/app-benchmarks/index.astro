---
import { Image } from "astro:assets";
import type {ImageMetadata} from "astro";
import RainbowIcon from "../../assets/rainbowIcon.svg";
import SuggestFormOpener from "../../components/atoms/SuggestFormOpener.astro";
import ThirdPlaceIcon from "../../assets/thirdPlaceAward.svg";
import AppLeaderboardCard from "../../components/atoms/cards/AppLeaderboardCard.astro";
import ItemsList from "../../components/molecules/ItemsList.astro";
import SubpageHero from "../../components/molecules/SubpageHero.astro";
import { appReviewCards } from "../../data/appReviewsData";
import Layout from "../../layouts/Layout.astro";
---

<Layout>
  <SubpageHero header="App benchmarks" />
  <section
    class="w-full box-border h-fit flex flex-col flex-nowrap justify-start items-center gap-10 p-5 sm:py-[60px] sm:px-8">
    <ItemsList header="Benchmarks" containerStyles="gap-3 sm:gap-3">
      {
        appReviewCards.map((app, idx) => (
            <AppLeaderboardCard
                key={`app-review-card-${idx}`}
                name="Rainbow Wallet"
                description="Rainbow is a fun, simple, and secure way to get started with crypto and explore Ethereum."
                overallScore={90}
                websiteLink="https://rainbow.me/"
                type="Wallet"
                viewReviewLink={`/app-benchmarks/${idx}`}>
              <Image src={RainbowIcon} alt={`${"app"}`} slot="appIcon" class="rounded-md" />
              {idx < 3 ? (<Image alt="third place" src={ThirdPlaceIcon} slot="placeIcon" />) : <p slot="placeIcon" class="w-8 pt-[7px] px-[11px] pb-[5px] text-sm leading-normal font-semibold text-muted-foreground">{idx+1}</p>}
            </AppLeaderboardCard>
        ))
      }
      <SuggestFormOpener whatsSuggested="app" suggestionStyle="outline" />
    </ItemsList>
  </section>
</Layout>
